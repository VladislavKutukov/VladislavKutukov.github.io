import React from "react";
import MessageExample from "./message";

export default {
  title: "antd/Сообщения",
  component: MessageExample,
};

const Template = (args) => <MessageExample {...args} />;

export const ПримерСообщений = Template.bind({});
